import React from 'react';
import { QueryBuilderProps } from '../types';

export const SearchQuerySpec: React.FunctionComponent<QueryBuilderProps> = props => {
  return <div>SearchQuerySpec</div>;
};
