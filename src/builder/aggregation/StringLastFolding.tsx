import React from 'react';
import { QueryBuilderProps } from '../types';

export const StringLastFolding: React.FunctionComponent<QueryBuilderProps> = props => {
  return <div>StringLastFolding</div>;
};
