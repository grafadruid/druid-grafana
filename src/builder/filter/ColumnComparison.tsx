import React from 'react';
import { QueryBuilderProps } from '../types';

export const ColumnComparison: React.FunctionComponent<QueryBuilderProps> = props => {
  return <div>ColumnComparison</div>;
};
