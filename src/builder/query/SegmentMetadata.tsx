import React from 'react';
import { QueryBuilderProps } from '../types';

export const SegmentMetadata: React.FunctionComponent<QueryBuilderProps> = props => {
  return <div>SEGMENTMETADATA</div>;
};
